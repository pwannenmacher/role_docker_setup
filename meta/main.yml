---
galaxy_info:
  role_name: role_docker_setup
  author: Paul Wannenmacher
  description: Installs Docker on Hosts, configures the daemon and sets up a database backup job
  company: Wannenmacher

  issue_tracker_url: https://git.wannenmacher.org/wannenmacher/ansible/-/issues

  license: MIT

  min_ansible_version: "2.9"

  platforms:
    - name: Ubuntu
      versions:
        - xenial
        - bionic
        - focal
        - jammy
    - name: Debian
      versions:
        - stretch
        - buster
        - bullseye

  galaxy_tags:
    - docker
    - ubuntu
    - debian

dependencies:
  - name: community.general
